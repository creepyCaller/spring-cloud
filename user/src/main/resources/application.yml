server:
  port: 8081
spring:
  application:
    name: UserProvider #当前服务注册在Eureka Server上的名称
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册中心访问地址
  instance:
    prefer-ip-address: true #将当前服务ip注册到Eureka Server